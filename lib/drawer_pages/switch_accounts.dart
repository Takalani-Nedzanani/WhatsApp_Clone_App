import 'package:flutter/material.dart';

class SwitchAccounts extends StatelessWidget {
  const SwitchAccounts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}